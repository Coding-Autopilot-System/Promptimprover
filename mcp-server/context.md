# Context: MCP Server (Legacy/Standalone)

Location: `mcp-server/`

## Purpose
This directory contains a legacy or standalone version of the MCP server. Most active development has moved to `universal-refiner/`.

## Module Instructions
1. **Migration Path**: Prefer making changes in `universal-refiner/` unless specifically targeting this standalone package.
2. **Compatibility**: Ensure any critical fixes here are also ported to the `universal-refiner`.
